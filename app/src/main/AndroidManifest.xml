<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Si usas cámara para fotos de plantas -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- AGREGAR ESTA LÍNEA -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- Para cámara frontal -->
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />

    <!-- Para flash de cámara -->
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PROYECTOFINAL6TO_ECOBOX"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Login -->
        <activity
            android:name=".presentacion.ui.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>

        <activity
            android:name=".presentacion.ui.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".presentacion.ui.ForgotPasswordActivity"
            android:exported="false" />

        <!-- ... -->
        <activity
            android:name=".presentacion.ui.ResetPasswordActivity"
            android:exported="true"
            android:launchMode="singleTask">

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- CONFIGURACIÓN HTTP PARA QUE GMAIL LO ACEPTE -->
                <data
                    android:host="www.ecobox-app.com"
                    android:pathPrefix="/recuperar"
                    android:scheme="http" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentacion.ui.MainActivity"
            android:exported="false" />
        <activity
            android:name=".presentacion.ui.AddPlantActivity"
            android:exported="false" />
        <!-- Dentro de <application> -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".presentacion.ui.PlantDetailActivity"
            android:exported="false" />

        <activity android:name=".presentacion.ui.AlertsActivity" />

        <activity android:name=".presentacion.ui.HistoryActivity" />
        <activity android:name=".presentacion.ui.LoginActivity" />
        <activity
            android:name=".presentacion.ui.AllEventsActivity"
            android:exported="false" />

    </application>
</manifest>